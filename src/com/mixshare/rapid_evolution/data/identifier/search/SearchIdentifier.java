package com.mixshare.rapid_evolution.data.identifier.search;

import com.mixshare.rapid_evolution.data.identifier.CommonIdentifier;

/**
 * Abstracted just in case, so serialization won't be messed up if this class is needed...
 */
abstract public class SearchIdentifier extends CommonIdentifier {

}
